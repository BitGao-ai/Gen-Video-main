Metadata-Version: 2.4
Name: cocf
Version: 0.1.0
Summary: COCF-SS-DCA: counterfactual, self-supervised video-diffusion acceleration framework
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: torch>=2.0
Requires-Dist: numpy
Requires-Dist: PyYAML
Provides-Extra: lmdb
Requires-Dist: lmdb; extra == "lmdb"
Provides-Extra: video
Requires-Dist: decord; extra == "video"
Requires-Dist: torchvision; extra == "video"
Provides-Extra: perception
Requires-Dist: torchvision; extra == "perception"
Provides-Extra: all
Requires-Dist: lmdb; extra == "all"
Requires-Dist: decord; extra == "all"
Requires-Dist: torchvision; extra == "all"

# COCF-SS-DCA

**基于轻量级反事实因果算力场与语义管锚定的高效视频扩散加速**
*(Causal-Counterfactual Compute Field with Semantic-Tube Anchoring)*

一个面向视频扩散 Transformer（DiT）的**即插即用加速层**。COCF 包裹一个**冻结的骨干模型**（Wan2.2、Wan2.1、HunyuanVideo 等），在去噪的每一步按跨帧**语义管**动态分配算力——感知上不重要、因果上稳定的区域廉价近似，因果上重要的区域保持全精度——从而在几乎不损失质量的前提下降低 FLOPs 与显存。

> 📖 面向开发者的逐文件代码说明见 [**CodeREADME.md**](./CodeREADME.md)。

---

## ✨ 核心特性

- **不是生成器，是加速层**：骨干全程冻结，只训练千万级的轻量插件。
- **四大创新协同**：因果算力场（L-COCF）+ 语义管锚定（STA）+ 可撤销锚定证书（RAEC）+ 跨模态语义守恒（CMSC）。
- **多骨干兼容**：通过「适配器 + 注册表」支持 Wan2.2（主力）/ Wan2.1 / HunyuanVideo，新增骨干只需写一个适配器。
- **训练省显存**：骨干冻结 + 潜变量/教师标签离线缓存，插件训练期 VAE 与文本编码器不占显存。
- **CPU 可跑通**：所有重型模型（VAE/SAM/DINOv2/CLIP/RAFT/骨干）依赖注入，内置确定性 Mock，无 GPU / 无权重也能构建、运行、测试。

---

## 🧠 核心思想

视频去噪中，并非每个区域每一步都值得全量计算：背景、静止物体、已收敛的细节可以廉价复用，而正在运动、交互、承载文字/人脸的因果关键区域才需要全精度。COCF 把这一直觉工程化为四步闭环：

1. **分组**（STA）：把逐帧感知（SAM 区域 + CLIP/DINO 特征 + 光流）链接成跨帧**语义管**，每管维护 7 维状态。
2. **定级**（L-COCF）：由「文本因果三元组 + 管状态」算出每管的**因果强度** `s = α·s_E + β·s_A + γ·s_T`，并预测各廉价动作的**反事实损伤** `(μ, σ)`。
3. **分配**（调度器）：在**动态单步算力预算** `B_t` 约束下，用贪心背包为每管选择四档动作之一：`FULL / LOWFREQ / INTERP / ANCHOR`。
4. **兜底**（RAEC）：为每个跳过决策算**误差证书**，按风险三档触发**回滚 / 边界修复 / 缓存刷新**——跳过决策在证书通过前从不是最终的。

全程再由 **CMSC** 的跨模态语义守恒损失约束加速结果不偏离全算力教师。

---

## 🏗️ 架构

> 📐 **完整算法架构图**(系统总览、单步 8 步闭环、L-COCF/STA/RAEC/CMSC 内部、三阶段训练、六级数据管线,共 8 张 Mermaid 图)见 [**ARCHITECTURE.md**](./ARCHITECTURE.md)。

```
                    ┌─────────────────────────────────────────────────────────┐
                    │                 Accelerator (core/)                      │
                    │   冻结骨干 + 4 个可学习插件 + 调度器 + 动作执行器          │
                    └────┬──────────┬──────────┬──────────┬───────────────────┘
                         │          │          │          │
                ┌────────┘   ┌──────┘    ┌─────┘    ┌─────┘
                ▼            ▼           ▼          ▼
          ┌──────────┐ ┌──────────┐ ┌────────┐ ┌──────────┐
          │  L-COCF  │ │   STA    │ │  RAEC  │ │   CMSC   │
          │  (§3)    │ │  (§4)    │ │  (§5)  │ │  (§6)    │
          └──────────┘ └──────────┘ └────────┘ └──────────┘
                    由 InferenceEngine (engine/) 逐步编排
```

| 创新 | 全称 | 包 | 职责 |
|------|------|----|------|
| **L-COCF** | 轻量级反事实因果算力场 | `cocf/lcocf` | 因果强度场 + 反事实损伤预测，驱动算力分配 |
| **STA** | 语义管锚定 | `cocf/tubes` | 逐帧感知 → 跨帧语义管 + 7 维状态 |
| **RAEC** | 可撤销锚定与误差证书 | `cocf/raec` | 误差证书 + 风险三档触发（回滚/修复/刷新） |
| **CMSC** | 跨模态语义守恒 | `cocf/cmsc` | 文本↔管对齐 + 6 项守恒损失 |

---

## 📦 安装

```bash
# 1) 克隆
git clone <repo-url> && cd pro_011

# 2) 安装依赖（核心）
pip install torch                                # ≥ 2.0
pip install scipy scikit-image einops pyyaml     # 算法/配置
pip install lmdb                                 # 处理存储（缺失自动回退 torch 文件）

# 3) 真实骨干（可选，CPU/mock 冒烟无需）
pip install diffusers transformers               # Wan2.2 / Wan2.1 / HunyuanVideo 适配器
```

> **纯 CPU 冒烟**只需 `torch`（+ scipy 可选）。真实视频生成才需要 `diffusers/transformers` 及对应模型权重与 GPU。

---

## 🚀 快速开始（CPU，mock 骨干）

无需任何权重，即可端到端跑通加速推理：

```bash
python scripts/inference/infer_single_video.py \
    --prompt "a cat jumping over a fence" \
    --backbone mock --steps 30 --quality balanced
```

或在 Python 中直接调用：

```python
import torch
from cocf.common.config import Config
from cocf.core.accelerator import Accelerator
from cocf.engine import InferenceEngine
from cocf.common.types import TokenGrid

config = Config()                                 # 默认 mock 骨干
config.backbone.name = "mock"
accelerator = Accelerator.from_config(config)     # 冻结骨干 + 4 插件（全 CPU）
engine = InferenceEngine(accelerator, config.engine, config.trigger)

prompt = "a cat jumping"
cond = accelerator.backbone.encode_text([prompt])
grid = TokenGrid(t=13, h=8, w=8)
z_init = torch.randn(1, grid.num_tokens, accelerator.token_dim)

result = engine.generate([prompt], z_init, grid, cond, accelerator.backbone)
print(result.efficiency_summary())                # 平均活跃 token 比、回滚数、修复数……
```

---

## 🎓 三阶段训练流程

一个 `processed_root` 贯穿三阶段：阶段 A 写、阶段 B/C 读。

### 阶段 A — 反事实教师数据生成（离线）

跑冻结骨干作为「教师」，测量每个廉价动作对最终视频的反事实损伤，写入**六级处理存储**。

```bash
python scripts/data/generate_counterfactual_data.py \
    --openvid-csv data/train/OpenVid-1M.csv \
    --data-root /datasets/OpenVid-1M \
    --processed-root ./LCOCF_OpenVid1M_Processed \
    --backbone wan22
```

### 阶段 B — 联合插件训练

骨干冻结，在缓存标签上联合训练 4 个插件，最小化
`L = L_cocf + λ_sta·L_tube + λ_cert·L_cert + λ_cmsc·L_cmsc + λ_cost·L_budget`。

```bash
python scripts/train/train_stage_b.py \
    --processed-root ./LCOCF_OpenVid1M_Processed \
    --backbone wan22 --epochs 10 --batch-size 32
```

### 阶段 C — 端到端微调（可选 LoRA）

跑完整加速引擎于真实 prompt，对全算力教师微调可微部件（L-COCF 头 + 可选最后 N 块 LoRA），损失
`L = λ_pixel·L_pixel + λ_cmsc·L_cmsc + λ_reg·L_reg`。

```bash
python scripts/train/train_stage_c.py \
    --processed-root ./LCOCF_OpenVid1M_Processed \
    --checkpoint-load ./checkpoints/stage_b/stage_b_best.pt \
    --use-lora --epochs 2
```

---

## 🎬 推理

```bash
python scripts/inference/infer_single_video.py \
    --prompt "a cat jumping" \
    --checkpoint ./checkpoints/stage_c/stage_c_epoch_2.pt \
    --backbone wan22 --steps 30 \
    --quality balanced \
    --output ./output.mp4
```

`--quality` 预设映射到动态预算下限 `b_min`：`fast=0.3`（跳得多）/ `balanced=0.5` / `quality=0.8`。

---

## 📁 目录结构

```
cocf/
├── common/        通用基础设施：类型、配置、注册表、省显存工具、日志
├── backbones/     骨干适配层（多模型兼容）：base / mock / wan22 / wan21 / hunyuan / transition
├── tubes/         STA 语义管锚定（§4）：区域→亲和度→匈牙利匹配→建管→7维状态→平滑损失
├── lcocf/         L-COCF 因果算力场（§3）：三元组→强度→动作映射→损伤预测→反事实检验
│   └── data.py    ★ 重点交付物：OpenVid-1M 六级处理存储参考实现
├── raec/          RAEC 可撤销锚定（§5）：误差证书→风险触发→局部修复→安全锚库
├── cmsc/          CMSC 跨模态语义守恒（§6）：文本↔管对齐 + 6 项守恒损失
├── scheduler/     调度器（§2.2/§7.3）：动态预算 + 贪心背包动作分配
├── engine/        加速推理引擎（§7.2）：8 步去噪循环 + 状态/轨迹
├── core/          顶层 Accelerator：把一切装配成单一枢纽
├── data/          数据管道（§1/§7.1）：OpenVid 清单 / 四级过滤 / 潜变量缓存 / LMDB 样本 / 分层采样
└── training/      三阶段训练（§7.1）：pipeline + stage_a/b/c + 教师前向 + LoRA
scripts/           CLI 入口：数据生成 / Stage-B / Stage-C / 推理
tests/             单元 + 集成测试
files/             设计文档（.docx）
```

---

## ⚙️ 配置

所有超参数集中在 `cocf/common/config.py`（分层 `dataclass`，每项标注来源章节）。可从 YAML/JSON 载入：

```python
from cocf.common.config import Config
config = Config.load("my_experiment.yaml")   # 未知键忽略，向前兼容
config.save("dump.yaml")
```

关键默认值（详见 [CodeREADME §6](./CodeREADME.md#6-关键公式与超参数汇总)）：

| 项 | 默认值 |
|----|--------|
| 强度分层阈值 θ1/θ2 | `0.66 / 0.33` |
| 风险触发 τ_low/τ_high | `0.40 / 0.80` |
| 动作成本乘子 | `FULL/LOWFREQ/INTERP/ANCHOR = 1.0/0.45/0.15/0.0` |
| 单步预算范围 b_min/b_max | `0.30 / 1.00` |
| 帧数 / 分辨率桶 | `49 (4k+1)` / `(480×832), (720×1280)` |

---

## 🧪 测试

```bash
python -m pytest tests/
```

- `tests/unit/` — 骨干适配器、L-COCF 损伤预测、数据生成、引擎
- `tests/integration/test_pipeline.py` — Mock 骨干上的全流水线端到端冒烟

---

## 📋 依赖

- Python ≥ 3.9
- PyTorch ≥ 2.0
- `scipy` / `scikit-image` / `einops`（算法）、`pyyaml`（配置）
- `lmdb`（处理存储，缺失自动回退 torch 文件）
- `diffusers` / `transformers`（**仅**真实骨干需要，惰性导入）

---

## 🚧 实现状态

**早期研究代码**，设计为「结构完整、CPU 可跑通、可测试」：

- 重型模型均为**依赖注入 + 确定性 Mock**，无 GPU/权重也能运行全栈。
- 部分组件为占位/简化（如时序插值恒等占位、边界融合固定 α、`data.py`/Stage-A 重型路径在 Mock 下合成占位数据、CMSC 局部项在加速循环内置 0）。
- 推理脚本使用硬编码 token 几何，`.mp4` 封装留有 TODO。
- 测试为最小覆盖。

占位点均以注释标明，替换为真实实现即可用于生产，不改变对外接口。

---

## 📚 延伸阅读

- [**CodeREADME.md**](./CodeREADME.md) — 逐子系统、逐文件的详细代码说明，含全部公式、数据结构与 8 步推理工作流。
- `files/` — 原始设计文档（`.docx`）。

---

*章节号（§2~§7）对应设计文档。本项目为训练 + 推理加速层，不含骨干模型权重。*
